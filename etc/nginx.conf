
server {
    listen 80
    server_name localhost

    location ^~ /uploads/ {
        root $HOME/box/web/uploads
    }

    location ~* ^.+\.\w+$ {

        root $HOME/box/web/public
    }

    location / {
        return 404
    }

}

